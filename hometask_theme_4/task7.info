# для создания алиаса:
# alias ll='ls -la' 
# Для зарепления алиаса необходимо:
#  nano ~/.bashrc
# дописать в конце файла: alias ll='ls -la'

# cd $(compgen -A directory so | head -n 1) - переход в первый найденный каталог начинабщийся на so
# compgen -A directory so выводит список каталогов на so
# а так, обычное автодополнение, при вводе директроии, через TAB работает